import { Meta } from '@storybook/react';
import React from 'react';

import SearchBar from '.';

export default {
  component: SearchBar,
  title: 'Postit/Atoms/SearchBar',
} as Meta;

export const Default = () => <SearchBar />;
